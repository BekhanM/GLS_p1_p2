package app;

import app.daos.PackageDAO;
import app.entities.Package;

public class Main {
    public static void main(String[] args) {
        //PackageDAO pdao = new PackageDAO();

        /*
        pdao.create(
                Package.builder()
                        .trackingNr("1234")
                        .senderName("Bekhan")
                        .receiverName("Otto")
                        .build());


        pdao.update(Package.builder()
                .id(1)
                .receiverName("Tom Cruise")
                .build());

*/
        //pdao.getByTrackingNumber("1234");

        //pdao.updatePackageStatus("1234", Package.deliveryStatus.PENDING);

        //pdao.delete("1234");

    }

}